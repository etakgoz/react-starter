import React from "react";
import style from "../styles/sample-header.less";

const Header = () => (
  <React.Fragment>
    <header className={style.header}>
        <h2>My Sample Header</h2>
    </header>
  </React.Fragment>
);

export default Header;
